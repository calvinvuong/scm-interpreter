class A {
      
  static function main() {
  var x = 6;
  var y = 10;
  return B.multiply(x, y);
  }

  function add(x, y) {
    return x + y;
  }
}

class B {

      function multiply(x, y) {
         return x * y;
	 }
	 }